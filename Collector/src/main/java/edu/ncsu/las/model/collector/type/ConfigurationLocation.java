package edu.ncsu.las.model.collector.type;

/**
 * Defines the location where a property must be implemented.
 * 
 */
public enum ConfigurationLocation {

		FILE,
		SYSTEM,
		DOMAIN,
		MANIFEST;   // put into the manifest file as a property...

}
